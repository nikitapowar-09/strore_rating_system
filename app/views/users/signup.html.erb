<h2>Sign Up</h2>
<%= form_with(model: User.new, url: sign_up_path, local: true) do |form| %>
  <%= form.text_field :name, placeholder: "Name" %>
  <%= form.email_field :email, placeholder: "Email" %>
  <%= form.password_field :password, placeholder: "Password" %>
  <%= form.submit "Sign Up" %>
<% end %>
